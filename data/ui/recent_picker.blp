using Gtk 4.0;
using Adw 1;

template $GradiaRecentPicker : Adw.Bin {
  child: Gtk.Grid item_grid {
    row-spacing: 10;
    column-spacing: 30;
    halign: center;
    margin-start: 50;
    margin-end: 50;
  };
}

